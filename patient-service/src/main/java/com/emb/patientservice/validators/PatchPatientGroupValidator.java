package com.emb.patientservice.validators;

/**
 * Will apply specific field validation, when a request to patch(partially update) a bean is received.
 * */
public interface PatchPatientGroupValidator {
}
